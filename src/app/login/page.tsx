import LoginPage from "@/components/pages/login/LoginPage";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <LoginPage />
    </div>
  );
};

export default page;
